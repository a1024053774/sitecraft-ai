# 基线记录 2026-09-15

- 工作目录：`/Users/luckye/Documents/Code/sitecraft-ai`
- 分支：`main`（跟踪 `origin/main`）
- origin：`https://github.com/a1024053774/sitecraft-ai.git`
- upstream：`https://github.com/redmaplewww/sitecraft-ai`
- HEAD：`5d30775169b7cba30df39ff47a8b766ff0ebd8bf`（`Initial public release`，2026-08-25）

本地实测环境（不是 CI 的 Node 22）：Node `v26.5.0`，npm `12.0.1`。仓库已有 `package-lock.json`；本地已有 `node_modules`，两次实测均未重新执行 `npm ci`。未提交、未删除 `artifacts/`。

## 扫描范围问题与工作树修复

首次实测（2026-09-15 13:12 +0800）时，工作树已存在 gitignored 目录 `artifacts/sitecraft-ai-pr4-b54eca6/`。根 `tsconfig.json` 的 `include` 为 `**/*.ts` / `**/*.tsx`，当时 `exclude` 只有 `node_modules` 和 `vendor`，因此 `npm run typecheck` 与 `npm run build` 会扫描该 PR4 参考副本。

该次真实结果：

- `npm run typecheck`（`tsc --noEmit`）退出码 `2`：744 条 TS 错误 / 227 个文件，全部位于 `artifacts/sitecraft-ai-pr4-b54eca6/`；仓库源码路径 0 条。
- `npm test` 退出码 `0`：`tests/site-operations.test.ts`，5 pass / 0 fail。
- `npm run build`（`next build`）退出码 `1`：Turbopack 编译成功，随后 TypeScript 检查失败（`Failed to type check.`），748 条 TS 错误 / 227 个文件，同样全部位于该 `artifacts/` 目录。

工作树修复：在根 `tsconfig.json` 的 `exclude` 中增加 `artifacts`，保留 `node_modules` 和 `vendor`。未删除 `artifacts/sitecraft-ai-pr4-b54eca6/`（复测时该目录仍在）。以下为增加 `exclude` 之后的复测，不能外推为 GitHub Actions 已跑过或已通过。

## 命令结果（exclude `artifacts` 后，2026-09-15 13:30 +0800）

### `npm run typecheck`

- 实际命令：`npm run typecheck`（脚本为 `tsc --noEmit`）
- 退出码：`0`
- 摘要：无 TypeScript 错误输出。

### `npm test`

- 实际命令：`npm test`（脚本为 `node --test --experimental-strip-types tests/*.test.ts`）
- 退出码：`0`
- 摘要：`tests/site-operations.test.ts`，`tests 5` / `pass 5` / `fail 0`，约 136ms。仍有 Node `MODULE_TYPELESS_PACKAGE_JSON` 警告（`package.json` 未声明 `"type": "module"`），不影响退出码。

### `npm run build`

- 实际命令：`npm run build`（脚本为 `next build`）
- 退出码：`0`
- 摘要：Next.js 16.3.1（Turbopack）编译成功；TypeScript 检查完成（Finished TypeScript in 1295ms）；生成 11 个静态页。路由表含 `/`、`/workspace`、若干 `ƒ /api/...` 动态路由。仍有警告：Next.js ignored `package-lock.json` in `/Users/luckye`（仓库外路径）；构建读取了 `.env.local`。
