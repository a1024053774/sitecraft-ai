node_modules
.next
dist
.sitecraft-data
.artifact-work
artifacts
outputs
out
.env*
!.env.example
*.log
*.tsbuildinfo
test-results/
playwright-report/
e2e/.cache/
e2e/test-results/

# 临时/一次性产物（2026-09-10）：避免被 `git add .` 误收
.tmp-*
check-tmp*.mjs
compact
.playwright-tmp/
.playwright-browsers/

# 成品展示与诊断截图（2026-09-10）：68MB 交付产物 + 根目录探针截图，属本地证据不做版本控制
成品展示/
/*.png

# 本轮客户旅程的证据截图（2026-09-12）：本地验收证据，不入库
.shots/

# 外部模板源码目录（2026-09-12）：
# 4.9M，且**内部含嵌套 .git**（`secttre/.git`）——直接 add 会变成嵌入式仓库/gitlink。
# 它不在 `open-source-templates/` 的 22 个 submodule 里，是独立拉下来的第三方行业模板。
vendor/industry-templates/

# ⚠️ `vendor/template-assets/` **不能忽略**（2026-09-12 纠正）：
# 它是运行时必需资源——`rewriteExternalResourceReferences(html, templateId)`
# 靠它把模板里的境外图片/CSS 换成本地路径，mirror 脚本注释明写「缺了它版式就坏」。
# 虽然理论上可由 `npm run templates:mirror --write` 再生，但那要联网下载境外资源；
# 交付环境不一定有网。**16M 换"模板页不依赖外网"，值得入库。**

# 编辑器本地设置（2026-09-12）
.vscode/settings.json
